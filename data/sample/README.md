# data/sample/

A tiny, committed CSV sample of the generated dataset so the repo is inspectable with zero
setup. Written by `aznamematch generate` (Phase 4). The full dataset (parquet) is
regenerable from the seed and is not committed.
