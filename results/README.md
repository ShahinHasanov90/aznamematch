# results/

The committed reference run (accuracy view + performance view + plots) will live here once
the evaluation harness (Phase 7) is built.

**Empty by design.** Per the project's zero-fabricated-numbers rule, no metric appears here
until it is produced by running the harness.
